# Set these in your shell; this file documents settings and is not auto-loaded.
CITEGUARD_MODE=neural
# Opt in for first-time model downloads. Default uses local cached models only.
CITEGUARD_ALLOW_DOWNLOAD=0
# Optional frontend override; default /api uses the same origin or Vite proxy.
VITE_API_URL=/api
